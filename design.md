# Design.md

## 1. 目的

この文書は、現在の在庫管理試作を
**単機能アプリ群から、共通基盤を持つ在庫管理プラットフォームへ育てるための設計書**として使う。

特に以下を目的とする。

- 今の試作でできること / できないことを混ぜない
- 次に何を作るべきかを、画面ではなく基盤単位で決める
- 仮想倉庫シナリオと設計をつなぐ
- Streamlit + SQLite 試作から、PostgreSQL 前提の本番設計へ移りやすくする
- Claude / Notion / GitHub に引き継ぎやすい共通文書にする

---

## 2. 現状整理

### 2-1. いまある土台
現在の試作には、以下の業務フローの土台がすでにある。

- 入庫
- 現在庫確認
- 出庫
- 移動
- 棚卸差異
- 引当管理
- 引当可能在庫
- 出荷確定
- 引当解除

また、DBの土台として以下が存在する。

- `inventory_transactions`
- `orders`
- `order_lines`
- `allocation_details`
- `items`
- `locations`

### 2-2. いま強い部分
- 在庫をイベントで持っている
- 現在庫を集計で出している
- 引当と出荷を分けている
- ロケーション別引当明細を持てる
- 部分出荷ができる
- 未出荷引当だけ解除できる
- priorityベース引当の土台がある

### 2-3. いま弱い部分
- 状態管理が薄い
- 承認フローがない
- 権限管理が薄い
- 監査ログの見える化が弱い
- 例外処理が標準化されていない
- 案件横断の影響表示が弱い
- 倉庫切替がない
- app.py と業務ページ群の役割が分かれており、全体のハブになっていない

---

## 3. 設計の基本方針

### 3-1. 画面追加より共通基盤を優先する
今後は「ページを増やす」より先に、以下を優先する。

1. マスタ管理
2. 権限管理
3. サービス層
4. 状態管理
5. 監査ログ

### 3-2. 在庫は事実、状態は解釈として分ける
- 在庫の事実は `inventory_transactions` に残す
- 業務状態は別の状態管理で持つ
- 承認や例外理由は監査ログに残す

つまり、
**数量の事実** と **業務上の状態** を分けて扱う。

### 3-3. 仮想倉庫シナリオで検証可能な設計にする
設計は、正常系だけでなく以下のようなズレに耐える必要がある。

- 引当したロケに現物が足りない
- 未入荷なのに出荷圧がかかる
- 出荷確定前に引当解除が必要になる
- 同じ商品に複数の出荷要求が重なる
- 急な優先出荷で既存計画を崩す
- 出荷作業中に割り込みが入る

### 3-4. 試作と本番を混ぜない
- 今のSQLite/Streamlitは試作の検証土台
- PostgreSQL前提の本番設計は別段で整理する
- ただし、試作の実装は本番へ持ち上げやすい形に寄せる

### 3-5. 命名と表示のルール
- 内部コード、状態コード、カラム名、テーブル名、関数名は英語でよい
- ただし、画面表示、帳票表示、利用者向け文言、運用上の見出しは日本語で表記する
- つまり、内部実装の識別子と、外部表示の文言を分けて扱う
- 例:
  - 内部状態コード: `PARTIALLY_ALLOCATED`
  - 画面表示: `一部引当`
  - 内部ロールコード: `WORKER`
  - 画面表示: `現場作業者`

### 3-6. 一覧から詳細への導線方針
- 状態一覧のような横断画面から詳細画面へ飛ぶ導線は、`order_id` / `line_id` を共通キーとして扱う
- 画面遷移時の受け渡しは、まずは Streamlit の `session_state` に詳細ターゲットを積む最小構成でよい
- これにより、初期導線は `状態一覧 -> 監査ログ` を優先しつつ、同じターゲット形式で `出荷確定` や `引当解除` へ拡張しやすくする

---

## 4. 目標アーキテクチャ

## 4-1. レイヤ構造

### UI層
- Streamlit画面
- 入力
- 表示
- フィルタ
- 確認メッセージ

### Application / Service層
- 在庫照会サービス
- 引当サービス
- 引当解除サービス
- 出荷確定サービス
- 移動サービス
- 差異記録サービス
- 承認サービス
- 監査記録サービス
- 状態遷移サービス

### Domain / Rule層
- 引当ルール
- 部分出荷ルール
- 未出荷解除ルール
- 例外出荷ルール
- 優先出荷ルール
- FIFO例外ルール
- 状態遷移ルール

### Data層
- SQLite（現試作）
- PostgreSQL（将来本番）

### 基本ルール
- 画面に業務ルールを散らさない
- 数量計算と状態遷移はサービス層へ寄せる
- DBアクセスは集約する

---

## 5. ドメインの整理

## 5-1. 核になる概念

### 在庫
- 商品
- ロケーション
- 倉庫
- ロット
- 在庫イベント

### 出荷
- 出荷指示ヘッダ
- 出荷指示明細
- ロケーション別引当明細
- 出荷確定実績

### 制御
- 状態
- 承認
- 例外
- 監査ログ
- ユーザー / ロール

---

## 5-2. 重要な分離

### 在庫数量
物理的にある / 動いた事実

### 引当数量
まだ他案件で使えない確保済み数量

### 出荷済数量
実際に出庫した数量

### 業務状態
未着手 / 作業中 / 保留 / 差戻し / 承認待ち など

この4つを混ぜないことを原則とする。

---

## 6. 状態管理設計

## 6-1. 状態管理が必要な理由
いまの試作は数量整合はある程度できるが、
途中状態が弱い。

そのため、以下をシステムで持てるようにする。

- 保留
- 差戻し
- 再引当待ち
- 一時中断
- 再開待ち
- 承認待ち

## 6-2. 管理対象

### 出荷指示状態
- NEW
- PARTIALLY_ALLOCATED
- ALLOCATED
- WORKING
- PARTIALLY_SHIPPED
- SHIPPED
- HOLD
- REWORK
- CANCELLED

### 明細状態
- LINE_OPEN
- LINE_PARTIALLY_ALLOCATED
- LINE_ALLOCATED
- LINE_REALLOC_PENDING
- LINE_PARTIALLY_SHIPPED
- LINE_SHIPPED
- LINE_HOLD
- LINE_CANCELLED

### 作業状態
- NOT_STARTED
- PICKING
- PAUSED
- READY_TO_RESUME
- COMPLETED

### 承認状態
- NOT_REQUIRED
- PENDING
- APPROVED
- REJECTED
- RESUBMIT_REQUIRED

## 補足: state_service 化の暫定評価メモ

現時点では、`state_service.py` を今すぐ新設する段階ではない。
今回の試作では、状態一覧を業務判断ハブとして整えつつ、画面側の小さな文言調整と導線整理で十分に改善できる。

### 1. 今の試作でできる
- 状態一覧側で、状態ごとの導線表示を調整する
- 文言、表示順、補助案内を改善する
- 出荷確定 / 引当解除後の戻り導線を整える

### 2. 少し足せばできる
- 状態別文言を小さな辞書にまとめる
- 導線ボタン文言を辞書化する
- 表示列順を共通定義に近づける

### 3. 共通基盤を作らないとできない
- 出荷可否、解除可否、承認可否を全画面で完全統一する
- 状態遷移の妥当性を一箇所で保証する
- 画面外処理や API 化に対応する

### 4. 本番向け設計として必要
- `state_service.py`
- `permission_service.py`
- `audit_service.py`
- `shipment_service.py`
- `release_service.py`
- PostgreSQL 前提のトランザクション管理
- 状態遷移ルール表

### 現時点の重複評価
- 状態一覧の `_can_show_ship_confirm` / `_can_show_release` / `_operation_hint` は、UI 表示用の可否判定としてはまだ許容範囲
- 出荷確定後の状態保存は `pages/8_出荷確定.py` 側にあり、引当解除後の次状態判定は `pages/9_引当解除.py` の `_next_state_after_release_from_line` に寄っている
- `core/db.py` の `get_enhanced_order_lines` / `get_all_line_state_rows` / `save_line_state` が、数量事実と最新状態表示の共通土台になっている
- ただし、状態判定・導線判定・操作可否判定がさらに増えるなら、次フェーズで service 層候補にする
- 暫定判断としては、今すぐの共通化よりも、まず UX と導線の整備を優先する

## 6-3. 最低限必要な項目
- `state_code`
- `state_reason`
- `changed_by`
- `changed_at`
- `hold_flag`
- `hold_reason`
- `approval_required`
- `approval_status`
- `free_note`

## 6-4. 実装方針
- まずは `orders` / `order_lines` に状態列を追加してもよい
- ただし履歴保持のため、最終的には `order_state_logs` を持つ
- 状態変更は直接更新ではなく、サービス経由にする


---

## 6-5. 状態一覧の業務ハブ方針

### 目的

状態一覧は、単に現在状態を表示する画面ではなく、  
出荷明細ごとの状態を起点に、次に確認すべき画面・次に行うべき操作へつなぐ  
**業務判断ハブ** として扱う。

この方針により、現場作業者やリーダーが、

- 今どの明細が止まっているか
- なぜ止まっているか
- 次にどの画面を見るべきか
- 操作してよい状態か
- 履歴確認が先か

を迷わず判断できるようにする。

---

### 背景

これまでの状態一覧は、主に以下を確認する画面だった。

- 最新状態
- 状態履歴
- 指示ID / 明細ID
- 数量情報
- 保留 / 承認状態
- 監査ログ / 出荷確定 / 引当解除への導線

ただし、状態ごとに次の行動が分かりにくい場合があった。

そのため、状態コードに応じて  
**おすすめ導線** を表示し、操作判断を補助する。

---

### 基本方針

状態一覧では、状態ごとに以下を表示する。

1. 状態に応じた日本語の案内文
2. 最優先の導線
3. 補助的な導線
4. 操作対象外の場合は履歴確認中心の案内

また、画面上の導線は出しすぎない。

- primary 導線は 1件のみ
- secondary 導線は 最大1件まで
- それ以外の導線は表示しない

これにより、現場での判断迷いを減らす。

---

### 状態別おすすめ導線

| 状態コード | 画面表示 | 主な意味 | primary導線 | secondary導線 |
|---|---|---|---|---|
| `ALLOCATED` | 引当済 | 出荷確定へ進める状態 | 出荷確定 | 監査ログ |
| `PARTIAL_SHIPPED` | 一部出荷 | 未出荷分が残っている可能性がある状態 | 出荷確定 | 監査ログ |
| `PARTIAL_ALLOCATED` | 一部引当 | 不足や例外確認が必要な状態 | 監査ログ | 引当解除 |
| `HOLD` | 保留 | 理由確認が最優先の状態 | 監査ログ | 引当解除 |
| `REALLOC_PENDING` | 再引当待ち | 再配分や解除確認が必要な状態 | 引当解除 | 監査ログ |
| `SHIPPED` | 出荷確定 | 操作ではなく履歴確認中心の状態 | 監査ログ | なし |
| `RELEASED` | 解除済 | 解除履歴確認中心の状態 | 監査ログ | なし |
| `UNALLOCATED` | 未引当 | 出荷確定 / 引当解除の対象外 | 監査ログ | なし |
| `SENT_BACK` | 差戻し | 操作前に理由確認が必要な状態 | 監査ログ | なし |
| `CANCELLED` | キャンセル | 操作対象外、履歴確認中心 | 監査ログ | なし |

---

### 表示ルール

状態一覧で明細を選択した時、状態履歴の下に案内文を表示する。

例:

- 引当済  
  「引当済みです。出荷確定へ進めます。」

- 一部出荷  
  「一部出荷済みです。未出荷分が残っている場合は、出荷確定画面で続きを確認してください。」

- 保留  
  「保留中です。まず監査ログで理由と履歴を確認してください。」

- 出荷確定  
  「出荷完了済みです。操作ではなく履歴確認が中心です。」

- 未引当  
  「未引当です。出荷確定や引当解除の対象外です。」

---

### 技術方針

現時点では、状態別おすすめ導線は `pages/11_状態一覧.py` 内で最小実装する。

理由:

- まだ state_service を作る段階ではない
- 過剰設計を避けるため
- 画面上の判断補助として先に検証するため
- 既存の `detail_target` / `session_state` 連携を活かせるため

使用する既存要素:

- `DETAIL_PAGE_PATHS`
- `_build_detail_target`
- `_can_show_ship_confirm`
- `_can_show_release`
- `detail_target`
- `return_focus`
- `state_list_filters`

---

### やらないこと

この段階では、以下はまだ行わない。

- DBスキーマ変更
- 新しい状態管理テーブルの追加
- `state_service` の新設
- 状態遷移ルールの全面共通化
- 権限管理の本実装
- 承認フローの本実装

あくまで、状態一覧の画面上で  
「次に見るべき画面」を分かりやすくすることを目的とする。

---

### 完了条件

状態一覧が以下を満たしていること。

- 明細選択時に状態に応じた案内文が出る
- primary 導線が1件に絞られている
- secondary 導線が最大1件に絞られている
- 操作対象外の状態では、履歴確認中心になる
- 既存の画面遷移が壊れていない
- フィルタ保持が壊れていない
- return_focus が壊れていない
- DB変更なしで成立している

---

### 現在の実装到達点

対象コミット:

`b9a9878`
`feat: refine state-based route UX for operational clarity`

この時点で、状態一覧は  
「最新状態を見る画面」から  
「状態を見て次の操作を判断する画面」へ進化した。

つまり、状態一覧は今後の在庫管理プラットフォームにおける  
**実務判断ハブ** として扱う。

---

## 7. 監査ログ設計

## 7-1. 目的
誰が、いつ、何を、なぜ変えたかを残す。

## 7-2. 監査対象
- 引当
- 再引当
- 引当解除
- 出荷確定
- 移動
- 差異記録
- 保留
- 保留解除
- 承認
- 却下
- 予定外ロケ使用
- FIFO逸脱
- 優先出荷割り込み

## 7-3. 監査ログ項目案
- `audit_id`
- `event_type`
- `event_at`
- `user_id`
- `role_code`
- `warehouse_code`
- `order_id`
- `line_id`
- `item_code`
- `location_code`
- `before_value_json`
- `after_value_json`
- `reason_code`
- `free_note`
- `approval_id`

## 7-4. 原則
- 数量変化だけでなく、理由も残す
- 例外処理は通常処理より強くログを残す
- 画面から追えることを目標にする

---

## 8. 権限設計

## 8-1. 想定ロール
- WORKER
- LEADER
- MANAGER
- ADMIN
- VIEWER

## 8-2. 分けたい操作
- 引当実行
- 引当解除
- 出荷確定
- 保留解除
- 差異確定
- 予定外ロケ出荷
- FIFO逸脱承認
- マスタ変更

## 8-3. 基本方針
- 作業者は通常操作中心
- リーダーは例外操作の承認 / 実行
- 管理者はマスタ / ルール変更
- 閲覧専用は更新不可

---

## 9. マスタ管理設計

## 9-1. 優先マスタ
- 商品マスタ
- ロケーションマスタ
- 倉庫マスタ
- ユーザーマスタ
- ロールマスタ
- 状態マスタ
- 例外理由マスタ
- 承認理由マスタ

## 9-2. ロケーションマスタで重要な項目
- `warehouse_code`
- `location_code`
- `location_type`
  - STORAGE
  - PICK_FACE
  - TEMP
  - HOLD
  - DEFECT
- `priority`
- `is_active`
- `valid_from`
- `valid_to`

## 9-3. 商品マスタで今後必要な項目
- 商品コード
- 商品名
- 単位
- 荷主
- ロット管理要否
- FIFO対象要否
- 前出し補充対象要否
- 使用可否

---

## 10. 画面別設計方針

## 10-1. app.py
### 現状
分析入口。
### 目標
プラットフォーム入口へ寄せる。
### 足すもの
- 倉庫切替
- ログインユーザー表示
- ロール表示
- 保留件数
- 承認待ち件数
- 例外件数
- 優先出荷件数

## 10-2. 入庫
### 足すもの
- 入荷予定との分離
- 実入庫時刻
- ロット / 入庫順
- 入荷遅延フラグ
- 誤ロケ訂正理由

## 10-3. 現在庫
### 足すもの
- 総在庫
- 引当済
- 引当可能
- 保留
- 差異中
- ロット別表示
- 入庫日表示
- ロケ種別表示
- 倉庫別表示

## 10-4. 出庫
### 足すもの
- 出荷指示状態
- 緊急フラグ
- 納期
- 保留理由
- 部分出荷可否
- リーダー確認要否

## 10-5. 移動
### 足すもの
- 移動理由区分
- 補充移動フラグ
- 一時置き / 保留ロケ対応
- 承認要否

## 10-6. 棚卸差異
### 足すもの
- 差異原因コード
- 発見者
- 発見時刻
- 差異確定承認
- 関連案件番号

## 10-7. 引当管理
### 足すもの
- 引当状態
- 再引当待ち状態
- 優先フラグ
- 影響案件表示
- 例外引当理由
- 承認付き再引当
- ロット / 入庫順情報

## 10-8. 引当可能在庫
### 足すもの
- 在庫内訳表示
- 倉庫別フィルタ
- 保留在庫分離
- 未入荷予定との分離表示
- 優先案件影響表示

## 10-9. 出荷確定
### 足すもの
- 確定前異常チェック
- 不足警告
- 予定外ロケフラグ
- FIFO逸脱理由
- 承認要否
- 一時保留
- 再開対象明細表示

## 10-10. 引当解除
### 足すもの
- 解除理由コード
- 解除影響案件表示
- 出荷済 / 未出荷の明確分離
- 再引当待ちへの接続
- 承認要否

---

## 11. DB拡張方針

## 11-1. すぐ足してよいもの（試作段階）
- `orders.status`
- `order_lines.status`
- `order_lines.hold_flag`
- `order_lines.hold_reason`
- `order_lines.approval_required`
- `order_lines.approval_status`
- `order_lines.updated_at`

## 11-2. 次に足したいテーブル
- `warehouses`
- `users`
- `roles`
- `user_roles`
- `order_state_logs`
- `audit_logs`
- `approval_requests`
- `exception_reason_master`
- `status_master`

## 11-3. PostgreSQL移行時の前提
- 外部キーの厳密化
- インデックス最適化
- warehouse_id導入
- transaction整合性強化
- JSON列の活用（監査ログ前後値など）

---

## 12. サービス層の設計案

## 12-1. 優先して分離したい関数群
- `inventory_service`
- `allocation_service`
- `shipment_service`
- `release_service`
- `movement_service`
- `count_service`
- `state_service`
- `approval_service`
- `audit_service`

## 12-2. 最低限の責務

### allocation_service
- 引当可能プール作成
- 引当計算
- 再引当
- 影響案件確認

### shipment_service
- 出荷確定前チェック
- ロケ別出荷実行
- 部分出荷
- 予定外ロケ出荷判定

### release_service
- 未出荷だけ解除できる判定
- 解除影響表示
- 再引当待ち更新

### state_service
- 状態遷移可否判定
- 状態変更記録
- 保留 / 差戻し / 再開

### audit_service
- 変更前後取得
- 操作種別記録
- 理由 / 承認ID 記録

---

## 13. 仮想倉庫シナリオとの接続

## 13-1. 先行検証対象
まず以下を先に回す。

- A-06 引当したロケに現物が足りない
- A-10 同じ商品に複数の出荷要求が重なる
- A-15 入荷遅延と出荷優先が同時発生する

## 13-2. この3つを先に選ぶ理由
- 状態管理が必要
- 例外処理が必要
- 影響案件表示が必要
- 引当可能在庫の定義が崩れやすい
- 現場判断とリーダー判断が分かれる

## 13-3. 検証時に必ず見ること
- 止めるべき所で止まるか
- 理由が残るか
- 再開できるか
- 誰の判断か残るか
- 数量整合が壊れないか

---

## 14. 実装フェーズ案

## Phase 1: 表示強化
### 目的
まず見える化だけ足す。
### 対象
- 現在庫の区分表示
- 引当可能在庫の内訳表示
- 引当解除の理由表示欄
- 出荷確定の異常警告表示
- app.py の全体サマリ

## Phase 2: 状態列追加
### 目的
画面外で持っている途中状態を、最小限DBで持つ。
### 対象
- order / line 状態
- hold_flag
- approval_status
- 状態変更理由

## Phase 3: 状態履歴 + 監査ログ
### 目的
変更追跡を可能にする。
### 対象
- order_state_logs
- audit_logs
- 変更前後保存

## Phase 4: 承認と例外
### 目的
例外を無言で通さない。
### 対象
- 予定外ロケ出荷
- FIFO逸脱
- 保留解除
- 優先差し込み

## Phase 5: サービス層整理
### 目的
画面依存を減らす。
### 対象
- 引当
- 出荷確定
- 引当解除
- 状態遷移
- 監査記録

## Phase 6: PostgreSQL再設計
### 目的
本番へ近づける。
### 対象
- 外部キー
- 倉庫ID
- ユーザー / ロール
- インデックス
- トランザクション整理

---

## 15. 今の試作でできる / 少し足せばできる / 基盤が必要

## 15-1. 今の試作でできる
- 在庫イベント記録
- 現在庫集計
- ロケーション別引当
- 部分出荷
- 未出荷引当解除

## 15-2. 少し足せばできる
- 在庫区分表示
- 理由入力の強化
- 出荷確定前警告
- 保留フラグ
- 再引当待ち表示

## 15-3. 共通基盤が必要
- 承認フロー
- 権限管理
- 状態履歴
- 監査ログの体系化
- 倉庫切替
- 案件横断の影響表示

## 15-4. 本番向け設計として必要
- PostgreSQL前提のキー設計
- 外部連携ID
- 倉庫 / 荷主 / ロット / 有効期限の厚み
- サービス層分離
- API前提構造

---

## 16. Definition of Done

この Design.md が効いている状態は、以下である。

- 次に作るべきものが画面名ではなく基盤名で言える
- 実装済みと未実装が混ざっていない
- 仮想倉庫シナリオと設計がつながっている
- 状態 / 承認 / 監査 / 例外が別物として整理されている
- PostgreSQL移行時に捨てずに使える設計になっている
- Claude / Notion / GitHub にそのまま渡せる

---
